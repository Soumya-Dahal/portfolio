# Backend Configuration
PORT=4000

# SMTP Configuration (optional - if not set, messages will be logged to file)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Frontend Configuration
FRONTEND_ORIGIN=http://localhost:3000
VITE_API_URL=http://localhost:4000

# Domain for Caddy TLS (production)
DOMAIN=
